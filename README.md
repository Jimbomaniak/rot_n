# task Caesar Cipher

---
### Prerequisites:
You need Python 3 or higher.

Run in terminal `$ pip install -r requirements.txt` to install 3rd party modules.

To run server `$ python manage.py runserver`

---
Main page '/rotn' (127.0.0.1:8000/rotn)


